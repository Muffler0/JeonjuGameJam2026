namespace Project.Core.Audio
{
    /// <summary>
    /// 사운드 키 상수 모음.
    /// 문자열을 직접 타이핑하지 않고 이 클래스를 거치면 오타가 컴파일 단계에서 걸러진다.
    /// SoundLibrary 에셋에 사운드를 추가할 때마다 여기에도 한 줄씩 추가할 것.
    /// (여기에 없는 키를 써도 동작은 하지만, 자동완성이 안 되고 오타 위험이 생긴다)
    /// </summary>
    public static class SoundKey
    {
        // ───── BGM ─────
        public const string ExampleBGM = "ExampleBGM";

        // ───── SFX : UI ─────
        public const string ExampleSFX = "ExampleSFX";

        // ───── SFX : Gameplay ─────
        
    }
}
